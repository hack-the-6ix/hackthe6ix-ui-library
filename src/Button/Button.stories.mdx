import { Meta, Title, Preview, Props } from '@storybook/addon-docs/blocks';
import vars from '../../helpers/getVariables';
import ToReact from '../../helpers/ToReact';
import Loader from '../../helpers/Loader';
import 'vue-awesome/icons/flag';
import Button from '.';

<div>
  {console.log(vars.tags.replace(/, /g, ',').replace(/"/g, '').split(','))}
</div>

<Meta title='Button' component={Button} />

<ToReact vue={Loader} />

<Title />

One clicky boi

<Props of={Button} />

## Variants

<Preview>
  <ToReact vue={Button} color='aqua' variant='solid'>
    Solid Variant (Default)
  </ToReact>
  <ToReact vue={Button} color='success' variant='outline'>
    Outline Variant
  </ToReact>
  <ToReact vue={Button} color='error' variant='ghost'>
    Ghost Variant
  </ToReact>
</Preview>

## Different colors

<Preview>
  {vars.tags
    .replace(/, /g, ',')
    .replace(/"/g, '')
    .split(',') // Normalize variables
    .filter((_) => !['background', 'disabled'].includes(_)) // Remove some colors
    .map((color) => (
      <div
        style={{
          width: 200,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'stretch',
        }}
        key={color}
      >
        <ToReact vue={Button} variant='solid' color={color}>
          {color}
          {color === 'text' && ' (Default)'}
        </ToReact>
      </div>
    ))}
</Preview>

## Disabled

<Preview>
  <ToReact vue={Button} variant='solid' disabled>
    Solid Variant
  </ToReact>
  <ToReact vue={Button} variant='outline' disabled>
    Outline Variant
  </ToReact>
  <ToReact vue={Button} variant='ghost' disabled>
    Ghost Variant
  </ToReact>
</Preview>

## Icons

<Preview>
  <ToReact vue={Button} variant='solid' color='light' icon={{ left: 'flag' }}>
    Left icon
  </ToReact>
  <ToReact vue={Button} variant='outline' color='navy' icon={{ right: 'flag' }}>
    Right icon
  </ToReact>
  <ToReact
    vue={Button}
    variant='ghost'
    color='pink'
    icon={{ left: 'flag', right: 'flag' }}
  >
    Both icon
  </ToReact>
</Preview>

## Loading

<Preview>
  <ToReact vue={Button} variant='solid' color='text' loading>
    Left icon
  </ToReact>
  <ToReact vue={Button} variant='outline' color='yellow' loading>
    Right icon
  </ToReact>
  <ToReact vue={Button} variant='ghost' color='success' loading>
    Both icon
  </ToReact>
</Preview>
